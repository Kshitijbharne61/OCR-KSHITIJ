# Input Images

Put product images here before running OCR.

Supported formats:
- JPG / JPEG
- PNG
- WEBP
- BMP
- TIFF

Run from the repository root:

```bash
python run_ocr.py
```

You can also pass an image path directly:

```bash
python run_ocr.py path/to/product.jpg
```