# OCR Output

This folder contains the files generated by the OCR engine.

For each input image, the OCR engine creates:

- `.txt` — extracted text only
- `.json` — structured OCR result with text, confidence, bounding boxes, and metadata
- `_regions.json` — OCR region information

Do not manually edit generated OCR output files.