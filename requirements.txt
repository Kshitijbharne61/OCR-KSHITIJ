# Recommended runtime: Python 3.12.x
# PaddleOCR 3.x local inference requires PaddlePaddle >= 3.0.
# Install PaddlePaddle from the official CPU wheel index on Windows:
# python -m pip install paddlepaddle==3.2.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/

paddleocr
opencv-python
numpy
pydantic>=2
